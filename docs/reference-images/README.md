# Reference Images (Local Only)

This directory is reserved for private style and composition references used by
the prompt-based map workflow.

Guidelines:

- keep third-party or personal reference images untracked unless licensing and
  provenance are explicit
- use them to anchor visual language and readability, not to duplicate someone
  else's exact layout
- treat paths in example briefs as placeholders that should be replaced with
  your own local references
